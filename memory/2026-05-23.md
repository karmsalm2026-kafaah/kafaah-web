# 2026-05-23

- Decreased font size of services mega menu items and technologies dropdown items in `Navbar.tsx` from `text-[11.5px]` to `text-[10.5px]`.
- Decreased services subtext from `text-[10px]` to `text-[9.5px]` for better visual balance and layout consistency.
- Redesigned the Experience page:
  - Simplified the entry point `src/app/experience/page.tsx` by delegating rendering entirely to a new client component to enable smooth layout transitions and interactions.
  - Overhauled `ExperienceClient.tsx` to set the Hero height to `90vh`.
  - Added dynamic micro-animations to all section headers, case-study cards, chemistry element cards, and geographic labels.
  - Re-engineered the coordinate radar map to be fully interactive: clicking or hovering on map nodes (Suez/Yanbu) displays a detailed localized popup card and cross-highlights the corresponding project card, and vice versa. Node clicks trigger smooth scrolling to card sections.
  - Fixed a runtime `IndexSizeError` in `ctx.arc` inside the radar pulse animation by replacing the sine wave radius with a linear ramp fraction, mathematically ensuring the radius is always positive.

