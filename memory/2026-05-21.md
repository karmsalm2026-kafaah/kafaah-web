# Memory: 2026-05-21

## Context & Decision

- The user requested hiding the floating "Built Inside Real Plants" card on the right on mobile and tablet versions of the website.
- The user also requested applying micro-animations to the texts (eyebrow, title, subcopy) in the Hero section.

## Actions Taken

- Modified `src/components/sections/Hero.tsx` to:
  - Add `motion` from `framer-motion`.
  - Implement two custom helper components: `HoverWords` and `HoverSubcopy` that split strings into words (or characters for Chinese) and wrap them with `motion.span` to apply spring hover scaling (`whileHover`), text shadow glow, and color shifting.
  - Apply Tailwind margins (`mr-[0.28em] rtl:mr-0 rtl:ml-[0.28em]`) directly to the animated `motion.span` items inside `<>` fragments to preserve standard LTR and RTL word-spacing and avoid whitespace collapsing of inline-block items.
  - Wrap the hero headline lines and subcopy in the newly created interactive hover components.
  - Implement an interactive eyebrow micro-animation where hovering over the eyebrow extends the gold accent line and shifts the text slightly to the right using Framer Motion transition states.
  - Hide the right-hand side floating "Built Inside Real Plants" card on screen widths below the desktop breakpoint (`lg` in Tailwind) using the CSS utility classes `hidden lg:flex`.
  - Decrease the eyebrow text font size above the main title to `text-[clamp(0.65rem,0.8vw,0.75rem)]` for non-English locales to create a cleaner, sleeker look.
  - Adjusted the paragraph description (`subCopy`) container width to `max-w-2xl` and restored `text-justify` alignment to stretch the text container and align its left and right boundaries perfectly with the main title's width.
  - Cleaned up unused imports in `Hero.tsx` (like `FileText`, `Activity`, `MapPin`, `Users`, `Briefcase`, `Layers`) to resolve ESLint build warnings.
- Run typecheck (`npx tsc --noEmit`) which compiled successfully with 0 errors.
- Verified that all lint warnings for `Hero.tsx` were completely resolved by ESLint.
- Verified that word spacing is correctly preserved on all screen sizes and languages (including RTL Arabic and English).
