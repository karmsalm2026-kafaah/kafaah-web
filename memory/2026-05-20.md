# Memory: 2026-05-20

## Context & Decision
- The user provided a Groq API key and requested that the project switch to using the Groq API instead of Google Gemini.
- The user pointed out that the chatbot content was unrelated to the site (it was referencing a software development agency instead of an industrial fertilizer consultancy) and was mixing languages. They also requested increasing the width of the chatbot window.

## Actions Taken
- Appended `GROQ_API_KEY` to `kafaah-web/.env.local`.
- Installed `@ai-sdk/groq` using npm.
- Updated `kafaah-web/src/app/api/chat/route.ts` to import `groq` from `@ai-sdk/groq` and use `groq('llama-3.3-70b-versatile')` as the LLM provider.
- Rewrote the entire chatbot knowledge base in `kafaah-web/src/lib/chatbotKnowledge.ts` to represent "Kafaah Industrial Solutions" (inorganic acid & fertilizer plant commissioning, troubleshooting, optimization) instead of the outdated software/web agency template.
- Enforced strict rules in the system prompt to restrict response output to pure Arabic (اللغة العربية الفصحى) and completely prohibit mixing foreign words or languages.
- Modified `kafaah-web/src/components/ChatWindow.tsx` to:
  - Add multi-language UI translation support for UI strings (header, placeholders, loading indicators, error messages) and initial welcome message in English, Arabic, and Chinese based on `useRole()`'s `locale`.
  - Dynamically shift layout direction (`dir="rtl"` vs `dir="ltr"`), input text alignments, and send button positioning (`left-2` vs `right-2`) based on the site's current locale.
  - Implement a client-side markdown link parser (`parseMessage`) to render clickable Next.js `<Link>` elements and bold text, ensuring customers can navigate directly to specific pages suggested by the AI assistant.
  - Send the user's active site `locale` in the API POST request body to `/api/chat`.
- Updated `kafaah-web/src/app/api/chat/route.ts` to extract `locale` from the request body and supply it to the new `getChatbotKnowledge(locale)` function.
- Updated `kafaah-web/src/lib/chatbotKnowledge.ts` to export `getChatbotKnowledge(locale)` which provides tailored system instructions in the user's current site language (Arabic, English, or Chinese) and includes exact markdown links to all key services, technologies, and contact page.
- Updated `kafaah-web/src/components/FloatingActions.tsx` to pass `key={locale}` to `ChatWindow` to force a clean re-initialization of the chatbot state whenever the site language is switched.
- Updated Suez and Yanbu project cards in `TrackRecord.tsx` and `i18n.ts` to be fully translated in English, Arabic, and Chinese, rendered dynamically.
- Localized the subCopy description text and placeholders in the `ContactCTA` component form to show corresponding English, Arabic, and Chinese placeholders based on `locale`.
- Verified type safety using `npx tsc --noEmit` which completed successfully with 0 errors.
- Initiated a production build (`npm run build`) to ensure full integration and compilation.
- Verified that the Next.js development server is still active and running at `http://localhost:3000`.
